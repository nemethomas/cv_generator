\documentclass[11pt,a4paper,sans]{moderncv}

% ---- Style & Farbe (classic = seriös, dunkelblau/grau) ----
\moderncvcolor{blue}
\moderncvstyle{classic}
\renewcommand*{\labelitemi}{\textcolor{color1}{\textbullet}}

% ---- Seitenränder ----
\usepackage[scale=0.8]{geometry}

% ---- von Pandoc benötigt ----
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% ---- Kopfzeile: Name, Titel, Kontakt ----
\name{$firstname$}{$lastname$}
$if(title)$\title{$title$}$endif$
$if(address)$\address{$address$}{}{}$endif$
$if(phone)$\phone[mobile]{$phone$}$endif$
$if(email)$\email{$email$}$endif$
$if(linkedin)$\social[linkedin]{$linkedin$}$endif$
$if(github)$\social[github]{$github$}$endif$
$if(photo)$\photo[64pt][0pt]{$photo$}$endif$

\begin{document}

\makecvtitle

$body$

\end{document}
